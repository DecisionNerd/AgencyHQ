import test from "node:test";

// Integration tests require a running Postgres instance.
// Start one with: pnpm db:up
// DATABASE_URL=postgres://agencyhq:agencyhq@127.0.0.1:5434/agencyhq_test

test("integration: placeholder (no-op)", () => {
  // Real integration tests will be added in Slice 3.
});
