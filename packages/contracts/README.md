# Contracts package

Versioned schemas for commands, events, ProcessDefinitions, StepContracts,
artifacts, and worker results. Compatibility rules will be added with the first
executable vertical slice.

