# Coordinator

Application boundary for domain commands, policy, scope, allocation, approvals,
acceptance, and reconciliation. It may call adapters only after durable domain
decisions. Initial implementation begins with the domain kernel in roadmap
slice 1.

