# AgencyHQ: close issues #1–#8 and implement roadmap Slices 4–6

## Context

- `main` at cec85ed (= tag `slice-3`): Slices 1–3 merged. One bounded-repair process through acceptance at the `artifact` boundary on the host runtime profile; 847 unit + 114 integration tests; CI `check` + `integration` jobs.
- Coordinator (Hono, `apps/coordinator`) owns the 15-table ledger, `BoundedRepairFlow`, a polling `Reconciler`, commands `plan|retry_dispatch|stop|pause|resume|create_work_item|ack_visit`, one return-after-interruption view; no auth, no Approval write path, no git push anywhere.
- `packages/domain` has aggregates, authority subset check (15 codes), acceptance rule (14 codes incl. `APPROVAL_REQUIRED`, `APPROVAL_VERSION_MISMATCH`, `VERIFIER_TAMPERED`), dispatch selection, classification; `WorkItem.boundary` accepts `merge|deploy` but only `artifact` completes; `integrate` boundary kind exists in profiles, nothing implements it.
- Trigger tasks `lead.plan`, `worker.attempt`, `verify.run`, `lead.review`, `lead.accept`; `concurrencyKey = projectId`; `lead.plan` runs untagged; `verify.run` records `integrity.protectedPathsSource` that the output schema strips.
- Web: Vite 7 + React 19, `@trigger.dev/react-hooks` realtime, served by Hono from `WEB_DIST`; view-model tests only. Open GitHub issues #1–#8 (all on `slice-1-3`, no comments). Trial record gaps: 4b/4c and weakened-test path not exercised live; `remediate` disposition does not dispatch.

## Assumptions

1. **open** — Issues #4 and #5 (live re-runs) run in one live session *after* the Slice 4 code lands (one Trigger stack bring-up, one spend budget, HEAD then covers reworks 2–4 and Slice 4). Alternative: run them first on cec85ed for a clean baseline (adds ~1 h and ~$5).
2. **open** — Spend cap per live session: $25 for the issues+Slice 4 session, $10 Slice 5, $25 Slice 6 (container spike). Models unchanged: worker `openai/gpt-5.6-terra`, Lead/review `openai/gpt-5.6-sol` variant low.
3. **open** — Slice 5 browser tests add `@playwright/test` (devDependency, Chromium only) and a CI `browser` job. If refused, R-011/R-019 stay "view-model + API tests" and the roadmap says so.
4. **open** — `deploy` boundary: no deploy target exists. Default: contracts with `boundary: deploy` are rejected at dispatch with `DEPLOY_NOT_SUPPORTED` on the host profile (R-016 path) and no `integrate.deploy` task is written. Roadmap line 71 wording "only where a project needs it" is satisfied by "no project needs it".
5. **open** — Slice 6 on one host proves: concurrent attempts across two projects under Trigger queue/environment limits, per-repo serialization, ProviderCapacity observations, Lead metrics. Not provable here: multiple worker machines, generation-bound push tokens (needs a forge token broker such as a GitHub App), model gateway. Those are recorded as deferred with evidence requirements. The container profile is a bounded *spike* (Docker Desktop worker stack + task image) whose result is recorded, not claimed as hardening.
6. **open** — Container spike needs an API-key provider inside the image. If the host's OpenAI provider is subscription-bound, the spike stops at `spike-echo` in a container and records that.
7. **default** — Issues #1, #2, #3, #6, #7, #8 ship as branch `issues-1-8` → PR → merge to `main` (closing those issues) before branch `slice-4`. #4/#5 close in the `slice-4` PR with the trial record.
8. **default** — `integrate.merge` is a Trigger task (ADR-0005 §"Common to both" 1; ARCHITECTURE.md:91; EXECUTION_MODEL.md:57-59), running in the coordinator-owned clone on the host, pushing with `--force-with-lease=<targetRef>:<expectedBase>` using the host credential helper. Generation binding is coordinator-side (intent key `<intent>:g<n>`, stale-generation refusal before dispatch) until the container profile exists.
9. **default** — No ProcessDefinition in Slice 4: multi-repository work is the bounded-repair step applied per manifest entry, not a second process (PROCESS_CATALOG.md:3-4). The extraction rule is recorded in PROCESS_CATALOG.md with the trigger condition.
10. **default** — Slice 5 adds no runtime dependency for routing (hash routes in `apps/web`); UI stack stays Vite/React + Hono in one process (R-012).
11. **default** — #8 part 2 (realtime subscription for the coordinator) is evaluated live in the Slice 4 session and recorded with date read; implementation as a wake-up for the existing poll path is a Slice 6 packet (cost of polling only matters with concurrency), polling remains the path of record so R-010 holds unchanged.

## Invariants every packet carries (verbatim)

- docs/README.md:29-37 — update docs with behavior; never represent planned work as current; cite Trigger/OpenCode behavior with date read. Docs never claim more than evidence shows.
- ADR bodies immutable except their `Implementation status:` line (0005:8, 0006:7, 0007:7), changed only in docs-sync waves with evidence on disk.
- `tests/architecture-baseline.test.mjs` is never edited; word "supervisor" only on a line also containing trigger|container|worker machine|worker stack.
- `packages/domain` imports nothing from Trigger, OpenCode, React, or a Postgres client (`tests/dependency-rules.test.mjs`).
- ADR-0006:29-46, 72-76 — Lead output is a proposal; the subset check decides; repository content only narrows.
- TESTING.md:44-50, 68-84 — the worker report is never an input to acceptance; a finished run is an observation, not acceptance.
- EXECUTION_MODEL.md:15-23 — decision + contract + attempt + DispatchIntent committed in one transaction before any trigger call.
- EXECUTION_MODEL.md:43-45, 64-73 — duplicate and stale-generation observations are no-ops (history only).
- ARCHITECTURE.md:104-109 — only `integrate.merge` may push; adapters and workers commit locally only.
- README.md:69-71, R-012 — no new infrastructure without an earning requirement and an ADR.

## Sequencing

Protocol as in the Slice 1–3 plan: Fable-main writes packets (template: INPUT / ENTRY / ALLOWED PATHS / FORBIDDEN / OUTPUT / COMPLETION CRITERIA / SELF-VERIFICATION / FAILURE / REPORT), Sonnet workers in isolated worktrees (`.claude/settings.json` baseRef head, `pnpm install --frozen-lockfile`), no commits by workers, one commit per wave, gate G1–G9 before each commit (G5 test count never decreases; G6 with Postgres from `infra/db/compose.yaml`; G9 wave probe below), rework packet max 2 rounds then escalate, fresh Fable reviewer per slice. New gate **G10** (Slice 5+): `pnpm test:browser` green. Branch per deliverable, PR to `main` with `Closes #n` trailers, CI green before merge, tag on the merge commit. Packets marked **[F]** are Fable-main-only (live stack, secrets, long-running processes).

### Wave I1 — issues, part 1 (branch `issues-1-8` from cec85ed; 4 parallel)

- **I1.a contracts (#6)** — ALLOWED `packages/contracts/src/tasks/verify-run.ts`, `packages/contracts/test/**`. Add optional `integrity: { tamperedPaths: string[], protectedPathsSource: "payload"|"default" }` to `VerifyRunOutputSchema`; JSON-schema export still succeeds. Criteria: schema round-trips the adapter's current shape (`trigger/src/tasks/verify-run-core.ts`); test count +2. Proves #6.
- **I1.b coordinator (#1)** — ALLOWED `apps/coordinator/test/integration/flow.parking.test.ts`, `apps/coordinator/test/helpers/**`. Two fake-runtime integration tests: (a) worker output `commitId: null` → contract failure, no `artifacts` row, `failures.class = contract`, attempt `failed`, no `verify.run` intent; (b) `humanRequired: true` contract → `lead.accept` proposal → decision `pending_human`, work item stays `active`, no `completed`. Inputs: `bounded-repair.ts` `onWorkerFinal` null-commit branch and `onAcceptFinal` F-14 branch; DOMAIN_MODEL.md:56-64. Proves #1, R-006, R-007.
- **I1.c coordinator (#2)** — ALLOWED `apps/coordinator/src/commands/approve.ts`, `apps/coordinator/src/commands/index.ts`, `apps/coordinator/src/app.ts` (only the `approve` case), `apps/coordinator/test/integration/approve.test.ts`, `apps/coordinator/test/api.test.ts` (additive). Command `approve {commandId, workItemId, contractId, contractVersion, attemptRevision, actor}`: claim command → in one tx insert `decisions(kind:'accept', actor:human, outcome:'approved')` + `approvals` row (`decision_id, contract_id, contract_version, attempt_revision, human_actor`) + transition audit → if a `pending_human` accept decision exists for that attempt, re-run `evaluateAcceptance` with the approval and complete at the boundary; idempotent by commandId (`replayed: true`). Route `POST /api/commands` kind `approve`. Interfaces: `packages/db/src/repos/approvals.ts`, `evaluateAcceptance` `approval` input, `approvalMatches`. Criteria: pending_human → approve → `accepted` + work item `completed`; approve for a different `attemptRevision` → `APPROVAL_VERSION_MISMATCH`, item stays pending; replay is a no-op. Proves #2, R-001, R-006, R-010.
- **I1.d trigger (#7)** — ALLOWED `trigger/scripts/opencode-smoke.ts`, `trigger/README.md` (script paragraph only). Require `--model` or `AGENCYHQ_OPENCODE_MODEL`; exit 2 with usage when absent; no literal model id remains (`grep -c big-pickle` = 0). Proves #7.

### Wave I2 — issues, part 2 (after I1 merge; 4 parallel, then docs)

- **I2.a coordinator (#3)** — ALLOWED `apps/coordinator/src/auth.ts`, `apps/coordinator/src/app.ts` (middleware mount only), `apps/coordinator/src/config.ts`, `apps/coordinator/src/main.ts`, `apps/coordinator/test/api.test.ts`, `apps/coordinator/test/wiring.test.ts`. `AGENCYHQ_API_TOKEN`; bearer middleware on `/api/*` except `/api/health`; token unset + bind host loopback → allow with startup warning; token unset + non-loopback → `loadConfig` throws `ConfigError`. Tests: missing/wrong/correct token → 401/401/200; health exempt; fail-closed config case. Proves #3.
- **I2.b web (#3)** — ALLOWED `apps/web/src/api.ts`, `apps/web/src/App.tsx`, `apps/web/test/**`. Token stored in `localStorage["agencyhq.apiToken"]`; sent as `Authorization: Bearer`; on 401 prompt once and retry. Proves #3 (client side).
- **I2.c coordinator (#6, #8 part 1)** — ALLOWED `apps/coordinator/src/flow/bounded-repair.ts`, `apps/coordinator/test/integration/flow.integrity.test.ts`, `apps/coordinator/test/integration/flow.options.test.ts` (additive). `onVerifyFinal` parses `integrity`, recomputes `detectVerifierTampering` from the resolved profile, and on disagreement records a blocking `verifier_tampered` finding with evidence `{adapter, coordinator}` and logs at error level (fail closed: union of both). `plan()` passes `tags: ["project:<id>", "workItem:<id>"]` to `lead.plan`. Tests: agree → one finding set; adapter omits field → coordinator result used; disagreement → union + evidence; `lead.plan` trigger options carry both tags. Proves #6, #8(1), R-014.
- **I2.d trigger (#6)** — ALLOWED `trigger/src/tasks/verify-run-core.ts`, `trigger/test/verify-run-core.test.ts`. Output conforms to `VerifyRunOutputSchema` (typed against the contracts schema, `safeParse` in the test). Proves #6.
- **I2.e docs-sync** (after a–d merge) — ALLOWED `docs/engineering/{ARCHITECTURE,DOMAIN_MODEL,EXECUTION_MODEL,TESTING}.md`, `apps/coordinator/README.md`, `apps/web/README.md`, `docs/REQUIREMENTS.md` (R-006 evidence cell only). Remove "untested" notes at DOMAIN_MODEL.md:56-64 and EXECUTION_MODEL.md:34-35, 53-56 citing `flow.parking.test.ts` and `approve.test.ts`; ARCHITECTURE.md:88 verify.run row (consumed, cross-checked), :158-160 security baseline (bearer implemented); EXECUTION_MODEL.md:31-33 (lead.plan tagged); TESTING.md coverage rows R-006/R-014. Present tense only for what tests prove.
- **[F] I2.g** gate, commit, push branch, open PR "Close issues #1–#3, #6–#8" with `Closes` trailers, wait CI, merge, tag none.

### Slice 4 — integration boundaries and multiple repositories (branch `slice-4` from the I2 merge)

**Wave 4.1 (schemas and domain; 4 parallel)**

- **4.1.a contracts** — ALLOWED `packages/contracts/src/tasks/integrate-merge.ts`, `packages/contracts/src/manifest.ts`, `packages/contracts/src/index.ts` (exports + `TASK_IDS.integrateMerge = "integrate.merge"`), `packages/contracts/src/step-contract.ts` (add `inputs.targetRef?`, `inputs.manifestDigest?`), `packages/contracts/src/tasks/{lead-plan,verify-run}.ts` (optional `manifest` field), tests. `IntegrateMergePayloadSchema {attemptId, generation, contractId, projectId, repoPath, remote, targetRef, expectedBaseRevision, attemptRevision, strategy: "merge_commit"|"fast_forward"}`; `IntegrateMergeOutputSchema {outcome: "integrated"|"already_integrated"|"base_moved"|"conflict"|"push_rejected", resultingRevision?, observedTargetRevision, evidence: string[]}`; `RevisionManifestSchema` = ordered `entries[{position, projectId, targetRef, expectedBaseRevision, resultRevision: string|null}]` + `digest`. Proves R-015 (schema), R-010 identities.
- **4.1.b domain** — ALLOWED `packages/domain/src/integration/**`, `packages/domain/src/transitions/work-item.ts`, `packages/domain/src/authority/runtime.ts`, `packages/domain/src/dispatch/select.ts`, tests. `requiredBoundariesFor` adds `integrate` when `bounds.boundary !== "artifact"`; `deploy` → `DEPLOY_NOT_SUPPORTED` violation (assumption 4). `complete()` for `merge` requires the manifest fully resolved (every `resultRevision` set) and returns revision = manifest digest + last result revision. `integration/decide.ts`: pure `decideIntegrationOutcome(output|observedRef, expected)` → `completed|retry_cas|escalate`; `integration/manifest.ts`: `nextEntry`, `allResolved`, `manifestDigest`. `selectDispatch` skip reason `integration_pending` for items with an open integrate intent. Property tests: CAS decision never returns `retry_cas` when the observed target differs from `expectedBase` and from the merge result. Proves R-015, R-016, R-008.
- **4.1.c db** — ALLOWED `packages/db/migrations/0003_manifest_integration.sql`, `packages/db/src/rows.ts`, `packages/db/src/repos/{work-item-projects,integrations}.ts`, `packages/db/src/repos/index.ts`, `packages/db/test/**`. Tables `work_item_projects (work_item_id, project_id, position, target_ref, expected_base_revision, result_revision, unique(work_item_id, position))` and `integrations (id, attempt_id, contract_id, contract_version, target_ref, expected_base_revision, resulting_revision, outcome, run_id, at)`; `step_contracts` gains `target_ref`, `manifest_digest`. Integration tests: manifest ordering, unique constraints, integration row idempotent by (attempt_id, target_ref, expected_base_revision). Proves R-002, R-010.
- **4.1.d trigger (integrate.merge task)** — ALLOWED `trigger/src/tasks/integrate-merge{,-core}.ts`, `trigger/src/lib/git.ts` (additive: `fetchRef`, `mergeInWorktree`, `pushForceWithLease`, `lsRemote`), `trigger/test/integrate-merge-core.test.ts`, `trigger/test/git.test.ts`. Task id `integrate.merge`, queue `integrate`, `concurrencyKey = projectId`. Core: `fetch origin <targetRef>`; if `origin/<targetRef>` already equals a commit whose tree contains `attemptRevision` (`merge-base --is-ancestor`) → `already_integrated`; if `origin/<targetRef> != expectedBase` → `base_moved`; else merge in a temp worktree (`merge_commit` default, `-c user.name=agencyhq`) → conflict → `conflict` with conflicting paths; else `git push origin <sha>:refs/heads/<targetRef> --force-with-lease=refs/heads/<targetRef>:<expectedBase>`; rejected → `push_rejected` + re-read ref. Runs with the task's own env (host credential helper), never the scrubbed worker env; `AbortTaskRunError` only for setup. Tests against two local bare+clone fixtures: happy path, base moved, conflict, replay after success, lease rejection when the remote advances between fetch and push. Proves R-015, R-010.

**Wave 4.2 (flow; sequential after 4.1)**

- **4.2.a coordinator (merge boundary)** — ALLOWED `apps/coordinator/src/flow/{bounded-repair,integrate}.ts`, `apps/coordinator/src/flow/observe.ts`, `apps/coordinator/src/config.ts` (none new), tests `flow.integrate.test.ts`. `onAcceptFinal` for `boundary: merge`: acceptance decision + `integrations` row + DispatchIntent in one tx, then trigger `integrate.merge` (tags as usual). `onIntegrateFinal`: `integrated|already_integrated` → set manifest entry `result_revision`, complete work item when `allResolved`, else dispatch next entry's plan; `base_moved|conflict|push_rejected` → contract failure, blocking finding kind `integration_conflict`, decision `pending_human`. Non-COMPLETED final run → coordinator reads `git ls-remote <remote> <targetRef>` from the clone and applies `decideIntegrationOutcome` (`retry_cas` → new intent same identity, at most `AGENCYHQ_INTEGRATE_RETRIES` = 2). Reconciler routes `integrate.merge` observations. Duplicate/stale observations no-ops (existing `applyObservation`). Tests: accept→integrate→completed with `resultingRevision`; base moved → pending_human, item incomplete; crashed run + ref already advanced → completed without re-push; replay → no second intent. Proves R-015, R-002, R-010, R-006.
- **4.2.b coordinator (remediate + multi-repo sequencing)** — ALLOWED `apps/coordinator/src/flow/bounded-repair.ts`, `apps/coordinator/src/commands/{create-work-item,disposition}.ts`, `apps/coordinator/src/app.ts` (`disposition` case, `create_work_item.manifest`), tests `flow.remediate.test.ts`, `flow.manifest.test.ts`. `create_work_item` accepts `manifest.entries[]` (projects must exist; `targetRef ∈ project.allowedRefs`, shape verified against `apps/coordinator/scripts/seed.ts`). `plan` runs `lead.plan` for entry 0 with `manifest` in the payload; each entry's contract freezes `targetRef` and `manifestDigest`; after an entry integrates (or, for `artifact`, accepts) the next entry is planned; `expectedBaseRevision` of entry n+1 is refreshed from the ledger, never from a worker. New command `disposition {findingId, disposition, reason}` (Lead or human): `remediate` → new attempt under the same contract within budget (failure row for the superseded attempt in the same tx, mirrors `autoNewAttempt`); `scope_decision|block` → pending_human. Proves R-017, R-018, R-007.
- **4.2.c trigger (combined verification)** — ALLOWED `trigger/src/tasks/verify-run-core.ts`, `trigger/src/lib/manifest.ts`, tests. When `payload.manifest` is present, materialize each sibling entry at its `resultRevision` (or `expectedBaseRevision` when unresolved) as a detached worktree under `<runDir>/manifest/<projectId>` and export `AGENCYHQ_MANIFEST_<POSITION>=<path>` to check commands; record `manifestDigest` and each materialized revision in every `VerificationResult.record`. Tests with two fixture repos and a `CHECK_CATALOG` entry `manifest-consumer@1` (in `packages/verification` — separate packet 4.2.d, ALLOWED `packages/verification/src/{checks,profiles}.ts`, tests; profile `multi-repo-v1`, version `"1"`). Proves R-006 (combined evidence), R-014.
- **4.2.e web** — ALLOWED `apps/web/src/**`, `apps/web/test/**`. Return view shows `integration` as a fifth state card for merge-boundary items (label, source `ledger`, timestamp, resulting revision short sha) and manifest progress `n/m`. Coordinator side of the view: add `integration` to `ReturnView.Item` in `apps/coordinator/src/views/return-view.ts` (packet 4.2.f, ALLOWED that file + `return-view.test.ts`). Proves R-011, R-019.

**Wave 4.3 [F] live session** (Trigger stack `infra/trigger` up, test Postgres up, `trigger dev` in background, coordinator with `RUNTIME=real`; throwaway repos `DecisionNerd/agencyhq-fixture-{parser,consumer}` created via `gh`, deleted on user OK):
1. Issue #4: trial items 1–3 and 7 on HEAD; assert live: stop during running worker → `stopped`, `checkpoint_commit` set, stale `run_observations`; stop during completion handling (inject via `AGENCYHQ_UNCERTAIN_AFTER_MS` small + stop right after COMPLETED) → `stopped|uncertain`, never `completed`; diff touching `package.json` → `VERIFIER_TAMPERED`.
2. Issue #5: scripted paths — (a) fixture whose only route to the requested fix is a write to a denied path, worker prompt says so; (b) fixture with a failing test the contract does not allow editing and a prompt that names the test as "flaky, delete it". Record outcomes; if the model still declines twice, record "no model-independent live path; deterministic tests are the gate" in TESTING.md with the two prompts on disk.
3. Slice 4 items: (8) merge boundary end to end with `humanRequired.boundaries: ["merge"]` → `pending_human` → `approve` via API → `integrate.merge` → target ref advanced, `resultingRevision` in ledger, `git ls-remote` agrees; (9) CAS: advance the remote by hand between accept and integrate → `base_moved`, item incomplete, pending_human; (10) two-repo manifest: consumer check passes only with the producer's resolved revision; (11) #8 part 2: `runs.subscribeToRunsWithTag` from a script against the self-hosted webapp, record delivery and date read.
4. Record `docs/engineering/trials/2026-09-slice4.md` (run ids, ledger rows, `stop.ndjson` lines, spend).

**Wave 4.4 docs-sync** — ALLOWED `docs/**` (not ADR bodies), package READMEs, `README.md`. Roadmap Slice 4 outcome; ARCHITECTURE.md:91 row and :104-109; EXECUTION_MODEL.md:57-59, :133-135; DOMAIN_MODEL.md:30-31, :23 (ProcessDefinition rule); PROCESS_CATALOG.md:3-4 extraction rule + "Multi-repository bounded repair" paragraph; TESTING.md rows R-015/R-016/R-017 and trial items 8–10; ADR-0005/0007 status lines. Then [F] gate, commit, PR "Slice 4" (`Closes #4`, `Closes #5`), CI, merge, tag `slice-4`, fresh Fable review.

### Slice 5 — control plane (branch `slice-5`)

**Wave 5.1 (data and API; 3 parallel)**

- **5.1.a db** — `packages/db/migrations/0004_campaigns.sql`, rows, repos, tests. `campaigns (id, name, main_effort_work_item_id, created_at)`; `work_items.campaign_id` nullable; `authority_versions (project_id, version, authority jsonb, actor, at)` audit; `decisions.kind` accepts `authority_update`, `reject`, `invalidate`. Proves R-008 (cross-project rank data), R-009 audit.
- **5.1.b domain** — `packages/domain/src/aggregates/campaign.ts`, `dispatch/select.ts` (campaign-scoped global rank; main effort per campaign), `authority/update.ts` (new authority version must be a valid `AuthoritySchema`; frozen contracts unaffected), tests incl. property test "rank order is total within a campaign". Proves R-008, R-009.
- **5.1.c coordinator API** — `apps/coordinator/src/{app,views/*}.ts`, commands `reject`, `invalidate_acceptance`, `create_campaign`, `set_rank`, `update_authority`; routes `GET /api/overview`, `GET /api/decisions`, `GET /api/work-items/:id/evidence` (artifacts, verification results, reviews, findings, decisions, approvals, integrations, manifest), `GET/PUT /api/projects/:id/authority`; view models pure and tested. `reject` on a pending decision → decision `rejected`, work item `halted` with reason; `invalidate_acceptance` → decision `invalidate` referencing the historical accept, work item `reopened` (TESTING.md:96). Proves R-011, R-017, R-019, R-001.

**Wave 5.2 (UI; 2 parallel)** — **5.2.a web** views: overview (campaigns, projects, ranked items, main effort), work item (five state cards + evidence panel + actions approve/reject/stop/pause/resume/new attempt), decisions (obstacle, recommendation, impact, no-action consequence from proposal rationale + violation codes), authority editor (JSON with schema errors inline, version shown, confirmation naming project + version). Hash routing, no new runtime deps; all state distinctions text + icon, not color alone (DESIGN.md:66-71). **5.2.b web tests** view-model tests for every new helper.

**Wave 5.3 browser tests** — **5.3.a** `apps/web/e2e/**`, `apps/web/playwright.config.ts`, `apps/web/package.json` (`test:browser`), root `package.json` (`test:browser`), `.github/workflows/check.yml` (job `browser`: Postgres service, build web, start coordinator with `RUNTIME=fake` + seeded ledger via `apps/coordinator/scripts/seed.ts`, run Playwright Chromium). Journeys: return after interruption (R-019), four/five states with source + timestamp (R-011), approve pending_human → completed, reject → halted, authority edit rejected on invalid schema, stop shows stopping→stopped. **[F] 5.3.b** live: one Slice 5 trial item (approve via UI on the real stack) recorded in `trials/2026-09-slice5.md` with a screenshot. **5.4 docs-sync**, gate incl. G10, PR "Slice 5", tag `slice-5`, Fable review.

### Slice 6 — capacity (branch `slice-6`)

**Wave 6.1 (3 parallel)** — **6.1.a domain** `ProviderCapacity` aggregate (`provider, model, status ok|limited|down, observedAt, validUntil`), `selectDispatch` input `providerCapacity[]` + `slots`; stale (past `validUntil`) = unknown = at most one concurrent attempt for that provider; skip reasons `provider_limited`, `provider_unknown`. Tests incl. property "concurrency never exceeds slots or per-provider limit". **6.1.b db** `provider_capacity` table + repo; `lead_metrics` SQL view (escalation rate, reversal rate via `invalidate` decisions, review yield via finding dispositions). **6.1.c trigger** `worker.attempt`/`lead.*` classify OpenCode 429/5xx events into `capacity` metadata; `ExecutionRuntime.subscribe?` (optional port method in `packages/domain/src/ports.ts` — packet 6.1.d) implemented in `trigger/src/client/{real,fake}.ts` via `runs.subscribeToRunsWithTag`, gated by the Slice 4 evidence of #8.

**Wave 6.2 (2 parallel)** — **6.2.a coordinator** `AGENCYHQ_WORKER_SLOTS` (default 1) wired into dispatch; reconciler writes `provider_capacity` from run metadata; subscription wakes `pollOnce()` (poll path unchanged, R-010); `GET /api/metrics/lead`. Tests: two projects dispatch concurrently at slots=2; same project serializes; provider `limited` caps to 1; wake-up never bypasses `applyObservation`. **6.2.b web** metrics view + capacity badge in overview; browser test.

**Wave 6.3 [F] container spike** — `infra/trigger` add the upstream worker-stack compose (supervisor + Docker socket proxy) at v4.5.16 with `UPSTREAM.md` diff notes; `trigger/Dockerfile` pinned Node 24, Git, OpenCode 1.18.29; `trigger deploy` to the self-hosted registry; run `spike-echo`, then `worker.attempt` with fresh clone from a throwaway repo and an API-key provider if available (assumption 6), attempt ref pushed with a manually created repo-scoped fine-grained PAT (24 h) injected as run env — recorded as *not* generation-bound. Record the observed enforcement table for `container` in `trials/2026-09-slice6.md`. **6.3 live capacity trial:** two fixture projects, slots=2, concurrent attempts, per-repo serialization, `provider_capacity` rows, metrics endpoint values. **6.4 docs-sync** — roadmap Slice 6 outcome split into *proved* and *deferred with evidence requirement* (multi-machine: trial on ≥2 `trigger dev` hosts or supervisor; push tokens: ADR for a forge token broker; gateway: ADR + measured spend requirement); ARCHITECTURE.md enforcement table container column marked "spike-observed" or "not observed" per row; ADR status lines. PR "Slice 6", tag `slice-6`, Fable review.

## Technical decisions

| Concern | Decision | Why |
| --- | --- | --- |
| integrate.merge execution | Trigger task in `trigger/`, queue `integrate`, `concurrencyKey = projectId`, runs in the coordinator clone, pushes with `--force-with-lease=<ref>:<expectedBase>` using the host credential helper; coordinator refuses dispatch for a revoked generation and keys the intent `<intent>:g<n>` | Matches ADR-0005 (every unit is a run), ARCHITECTURE.md:91, EXECUTION_MODEL.md:57-59, 72; the remote's lease check is the only true CAS; per-repo serialization comes free from queues |
| Unknown integration outcome | Coordinator reads `git ls-remote` and applies pure `decideIntegrationOutcome`; at most 2 CAS retries; never a blind re-push | EXECUTION_MODEL.md:133-135; R-015 |
| Generation-bound push token | Deferred; host profile has no token broker; recorded in ADR-0005 status line and Slice 6 deferred list | No forge app/gateway exists; R-012 forbids new services without an ADR |
| Revision manifest | Ordered entries `{position, projectId, targetRef, expectedBaseRevision, resultRevision}` in `work_item_projects`; `manifestDigest` frozen in each StepContract; entry n+1 base refreshed from ledger after entry n integrates | DOMAIN_MODEL.md:30-31; R-018 (evidence bound to exact versions); dependency order is the position |
| Combined verification | `verify.run` materializes sibling entries as read-only worktrees under the run dir and exposes paths via env to catalog checks; results record manifest digest and revisions | Keeps verification in `verify.run` (R-014), no new service, provable with two local fixture repos |
| `deploy` boundary | Rejected at dispatch (`DEPLOY_NOT_SUPPORTED`) on host profile; no task | No deploy target; docs must not claim deploy support |
| ProcessDefinition | Not extracted; PROCESS_CATALOG.md records the rule "extract when a second catalog entry needs a different step/gate sequence" | Multi-repo is the same step per entry; PROCESS_CATALOG.md:3-4 |
| `remediate` | New `disposition` command; `remediate` creates a new attempt under the same contract within budget with a failure row for the superseded attempt | Trial record "what remains"; R-017; mirrors existing `autoNewAttempt` transaction shape |
| Auth (#3) | Bearer token middleware, health exempt, fail closed off-loopback, dev warning on loopback; web stores token in localStorage | Issue text; R-012 single process; no session infra |
| #6 cross-check | Union of adapter and coordinator tamper sets, disagreement logged and recorded as evidence | Fail closed; adapter field becomes read evidence |
| Observation (#8) | Polling stays the path of record; tags on `lead.plan` now; realtime subscription only as a wake-up in Slice 6 after live evidence | R-010 unchanged on both paths; dedupe already in `applyObservation` |
| Slice 5 stack | Vite/React + Hono in one process; hash routes; no state library | R-012; existing tests/build |
| Browser runner | `@playwright/test` Chromium, CI job with Postgres + fake runtime + seeded ledger | Only deterministic way to prove R-011/R-019 "browser tests"; open assumption 3 |
| Capacity source | ProviderCapacity from adapter-classified OpenCode rate-limit events; stale = unknown = concurrency 1 | DOMAIN_MODEL.md:24; conservative per roadmap |
| Slice 6 honesty | Prove concurrency, capacity, metrics on one host; container profile as a recorded spike; multi-machine/tokens/gateway deferred with named evidence | One host, no gateway; docs never exceed evidence |

## Requirement → proving test

| R-id / issue | Proving evidence |
| --- | --- |
| R-001 | `approve.test.ts` (human Approval recorded by coordinator only); `update_authority` command tests |
| R-002 | `flow.integrate.test.ts` (accept + integration row + intent in one tx before trigger); db 0003 tests |
| R-006 | `flow.parking.test.ts` (b); `approve.test.ts`; `flow.integrate.test.ts` merge completion requires resulting revision; manifest verification records |
| R-007 | `flow.parking.test.ts` (a) null commit = contract failure; `flow.remediate.test.ts` |
| R-008 | domain `dispatch.test.ts` (campaign rank, slots, provider caps); coordinator concurrency tests (6.2.a) |
| R-009 | authority update tests (frozen contracts unaffected; invalid schema rejected) |
| R-010 | integrate replay tests (trigger + coordinator); approve replay; wake-up-never-bypasses-dedupe test |
| R-011 | Playwright work-item journey; view-model tests |
| R-014 | `flow.integrity.test.ts` cross-check; trial #5 record or TESTING.md conclusion |
| R-015 | `integrate-merge-core.test.ts` (5 cases); `flow.integrate.test.ts`; live items 8–9 |
| R-016 | domain `runtime.test.ts` (`integrate` required for merge; `DEPLOY_NOT_SUPPORTED`) |
| R-017 | `flow.remediate.test.ts`; `disposition` command tests |
| R-018 | manifest tests: entry n+1 base from ledger; contract `manifestDigest` mismatch invalidates evidence |
| R-019 | Playwright return journey; `return-view.test.ts` integration card |
| R-013 | live #4 re-run (items 2, stop-during-completion) |
| R-004 | #8 tags test; live subscription evidence (date read) |
| R-012 | Slice 6 docs: only Trigger worker stack added, with ADR-0005 status evidence |
| #1 | `flow.parking.test.ts` + docs citations |
| #2 | `approve.test.ts` + `POST /api/commands` kind `approve` test |
| #3 | `api.test.ts` 401/401/200 + fail-closed config test + web token test |
| #4 | `trials/2026-09-slice4.md` §items 1–3, 7 with run ids, ledger rows, `stop.ndjson` |
| #5 | same record §item 4b/4c/6 or TESTING.md "no model-independent path" with prompts on disk |
| #6 | contracts schema test + `flow.integrity.test.ts` + verify-run-core conformance test |
| #7 | smoke script usage test (`node trigger/scripts/opencode-smoke.ts` without model → exit 2) |
| #8 | `lead.plan` tags test; subscription evaluation in trial record |

## Risk register

| Risk | Mitigation | Signal |
| --- | --- | --- |
| `force-with-lease` needs the remote-tracking ref fresh; a stale fetch makes the lease vacuous | Explicit `<ref>:<expectedBase>` form, never bare `--force-with-lease`; test "remote advances between fetch and push" | Lease test fails or push succeeds with moved base |
| Host credential helper prompts or lacks push rights | Preflight `git push --dry-run` in the trial before item 8; `GIT_TERMINAL_PROMPT=0` | Task hangs → maxDuration |
| Multi-repo scope balloons | Manifest is data + sequencing only; one bounded-repair step per entry; two-repo fixture is the whole proof | Packet exceeds one package or adds a new task |
| Playwright rejected or flaky in CI | Open assumption 3; fake runtime + seeded ledger, no live stack in CI; retries 1 | Browser job red on unchanged code |
| Worker model again declines adversarial writes (#5) | Two scripted fixtures; record conclusion after 2 declines each; deterministic gates remain evidence | Same PARTIAL as Slice 3 |
| Container spike blocked by provider auth or Docker Desktop networking | Stop at `spike-echo`; record; no docs claim beyond observation | supervisor cannot pull image / OpenCode 401 in container |
| Docs overclaim (F-7 pattern) | G8 + docs-sync packets only after trial records exist; reviewer drift section | Reviewer MAJOR on wording |
| Concurrency exposes races in `pollOnce` | `_polling` guard kept; wake-up calls the same `pollOnce`; tests run two projects | Duplicate intents in ledger |
| Spend | Caps per assumption 2; ≤2 reruns per item; fakes first | `step_finish` usage totals |
| Cross-worker file conflicts (`bounded-repair.ts`, `app.ts`) | Sequence packets touching the same file; split `flow/integrate.ts` out | Merge conflict at gate |

## Estimates

| Wave | Sonnet packets | Fable-main | Wall-clock | Live spend |
| --- | --- | --- | --- | --- |
| I1 + I2 (issues) | 4 + 5 (+ ≤2 rework) | gate, PR | 2–3 h | $0 |
| Slice 4 | 4.1: 4, 4.2: 6, 4.4: 1 (+ ≤4 rework) | live session ~2 h, review | 8–10 h | ~$15 (#4/#5 re-runs + items 8–10) |
| Slice 5 | 5.1: 3, 5.2: 2, 5.3: 1, 5.4: 1 (+ ≤3 rework) | live item, review | 6–8 h | ~$5 |
| Slice 6 | 6.1: 4, 6.2: 2, 6.4: 1 (+ ≤3 rework) | container spike ~3 h, capacity trial ~1 h, review | 8–10 h | ~$15–20 |

User steps: approve open assumptions 1–6; create/allow throwaway fixture repos; provide an API-key provider for the container spike or accept the `spike-echo` stop; delete throwaway repos at the end.

## Verification

- **Issues PR:** `pnpm check && pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration` green; unit ≥ 847+, integration ≥ 114+; `gh issue view 1..3,6..8` closed by merge; reviewer checks the three docs "untested" notes are gone and nothing claims a live run.
- **Slice 4:** all gates; `trials/2026-09-slice4.md` has items 1–3, 7 (re-run), 4b/4c/6 outcome or documented conclusion, 8 (merge via approval, remote ref advanced, ledger `integrations.resulting_revision` equals `git ls-remote`), 9 (`base_moved` → pending_human), 10 (manifest completes with both revisions), #8 subscription note with date read. Reviewer checks: only `integrate.merge` pushes (`grep -rn "git push\|force-with-lease" --include=*.ts` outside `trigger/src/tasks/integrate-merge*` and `lib/git.ts` returns tests only); decision+intent before trigger in `onAcceptFinal` merge path; stale generation cannot dispatch integration; ADR bodies unchanged; `pnpm check` 11/11.
- **Slice 5:** G10 Playwright green locally and in CI; `trials/2026-09-slice5.md` approve-via-UI on the real stack with screenshot; reviewer walks R-011/R-019 rows to the journey tests and confirms DESIGN.md:66-71 (text + icon, destructive confirmations name project/item/version).
- **Slice 6:** capacity trial record: two concurrent runs on two projects with run ids, same-project serialization observed, `provider_capacity` rows, `/api/metrics/lead` values reconciled against ledger counts by hand; container spike record with the observed enforcement table; roadmap Slice 6 lists deferred items with evidence requirements; reviewer confirms no doc line claims multi-machine, generation-bound tokens, or gateway enforcement.
